import { ENCRYPTION_PASSWORD, PRIVATE_KEY_ENCRYPTION_STRING } from '@env';

const config = {
  encryptionPassword: ENCRYPTION_PASSWORD,
  privateKeyEncryptionString: PRIVATE_KEY_ENCRYPTION_STRING,
};

export default config;
